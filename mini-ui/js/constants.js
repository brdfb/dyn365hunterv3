// Constants - Configuration values

/**
 * Debounce delay for search input (milliseconds)
 */
export const DEBOUNCE_DELAY = 400;

/**
 * Toast notification auto-dismiss duration (milliseconds)
 */
export const TOAST_DURATION = 4000;

/**
 * Duplicate request prevention window (milliseconds)
 * If same request is made within this window, it will be skipped
 */
export const DUPLICATE_REQUEST_WINDOW = 500;

/**
 * Refresh delay after form submission (milliseconds)
 * Used to wait for backend processing before refreshing leads list
 */
export const REFRESH_DELAY = 1000;

