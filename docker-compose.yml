services:
  postgres:
    image: postgres:15-alpine
    container_name: dyn365hunter-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-dyn365hunter}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password123}
      POSTGRES_DB: ${POSTGRES_DB:-dyn365hunter}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-dyn365hunter}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - dyn365hunter-network

  redis:
    image: redis:7-alpine
    container_name: dyn365hunter-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - dyn365hunter-network

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dyn365hunter-api
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://dyn365hunter:password123@postgres:5432/dyn365hunter}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      API_HOST: ${API_HOST:-0.0.0.0}
      API_PORT: ${API_PORT:-8000}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ENVIRONMENT: ${ENVIRONMENT:-development}
      # IP Enrichment (for testing - enable in .env for production)
      HUNTER_ENRICHMENT_ENABLED: ${HUNTER_ENRICHMENT_ENABLED:-true}
      MAXMIND_CITY_DB: ${MAXMIND_CITY_DB:-app/data/maxmind/GeoLite2-City.mmdb}
      IP2LOCATION_DB: ${IP2LOCATION_DB:-app/data/ip2location/IP2LOCATION-LITE-DB11.BIN}
      IP2PROXY_DB: ${IP2PROXY_DB:-app/data/ip2proxy/IP2PROXY-LITE-PX11.BIN}
      # Azure AD (G19) - Microsoft SSO
      HUNTER_AZURE_CLIENT_ID: ${HUNTER_AZURE_CLIENT_ID:-}
      HUNTER_AZURE_CLIENT_SECRET: ${HUNTER_AZURE_CLIENT_SECRET:-}
      HUNTER_AZURE_TENANT_ID: ${HUNTER_AZURE_TENANT_ID:-}
      HUNTER_AZURE_REDIRECT_URI: ${HUNTER_AZURE_REDIRECT_URI:-http://localhost:8000/auth/callback}
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
      - ./mini-ui:/app/mini-ui
      - ./alembic:/app/alembic
      - ./app/data:/app/app/data:ro  # IP enrichment DB files (read-only)
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthz"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - dyn365hunter-network

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dyn365hunter-worker
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://dyn365hunter:password123@postgres:5432/dyn365hunter}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ENVIRONMENT: ${ENVIRONMENT:-development}
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
    command: celery -A app.core.celery_app.celery_app worker --loglevel=info --concurrency=5 --max-tasks-per-child=50 --time-limit=900 --soft-time-limit=870
    networks:
      - dyn365hunter-network

volumes:
  postgres_data:
  redis_data:

networks:
  dyn365hunter-network:
    driver: bridge

